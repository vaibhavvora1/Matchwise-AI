# Backend API Base URL
# For local development (Express server on port 3000):
VITE_API_URL=http://localhost:3000

# For production deployment (e.g. AWS Application Load Balancer or custom backend domain):
# VITE_API_URL=http://MatchwiseAI-1052301444.ap-south-1.elb.amazonaws.com
# Or leave empty / unset when frontend is served from the same container/origin (uses relative /api):
# VITE_API_URL=
